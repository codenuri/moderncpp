#include <iostream>

int main()
{
	int n = 0;
	double d = 3.4;

	// 포인터 변수를 만드는 방법
	// => 선언하는 변수 이름 앞에 * 를 붙이고
	// => 우변 변수앞에는 & 를 붙여서 주소를 꺼낸다.
	int *p1 = &n;
	double *p2 = &d;

	int x[3] = { 1,2,3 };

	// 위 규칙을 잘생각해서, 배열 x 의 주소를 담는 포인터 변수 p3를 
	// 만들어 보세요

}